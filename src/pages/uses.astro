---
// /uses (EN). Thin wrapper — the shared, foundation-owned view resolves lang
// from Astro.currentLocale and renders the localized content.
import UsesView from '../components/UsesView.astro';
---

<UsesView />
